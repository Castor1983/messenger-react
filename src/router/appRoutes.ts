export const appRoutes:{MAIN: string, REGISTER: string, LOGIN: string, CHAT: string, HOME: string} = {
    MAIN: '',
    HOME: '/home',
    REGISTER: '/register',
    LOGIN: '/login',
    CHAT: '/login/chat',
}
